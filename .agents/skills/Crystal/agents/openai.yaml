interface:
  display_name: "Crystal"
  short_description: "Repo-specific patterns and workflows for Crystal"
  default_prompt: "Use the Crystal repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true